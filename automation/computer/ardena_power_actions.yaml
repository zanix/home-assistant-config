---
id: ardena_power_actions
alias: '[Ardena] Power Actions'
description: 'Actions to run when Ardena is on/off'

trigger:
  - platform: state
    entity_id: binary_sensor.ardena_power_status

action:
  - choose:
      - conditions:
          - condition: state
            entity_id: binary_sensor.ardena_power_status
            state: 'on'
        sequence:
          - service: switch.turn_on
            entity_id: switch.computer_exhaust_fan

      - conditions:
          - condition: state
            entity_id: binary_sensor.ardena_power_status
            state: 'off'
        sequence:
          - service: switch.turn_off
            entity_id: switch.computer_exhaust_fan
