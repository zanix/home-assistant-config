---
# HACS (Home Assistant Community Store)
#
# This is a manager for your custom integration (components) and
# plugin (lovelace elements) needs.
# It can help you download and update elements and discover new awesome stuff.
#
# https://custom-components.github.io/hacs/
#
hacs:
  token: !secret github_access_token
