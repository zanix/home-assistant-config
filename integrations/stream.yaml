---
# The stream integration provides a way to proxy live streams through
# Home Assistant. The integration currently only supports proxying H.264 source
# streams to the HLS format and requires at least FFmpeg >= 3.2.
#
# https://www.home-assistant.io/integrations/stream/
#
stream:
