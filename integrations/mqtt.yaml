---
# MQTT (aka MQ Telemetry Transport) is a machine-to-machine or
# “Internet of Things” connectivity protocol on top of TCP/IP. It allows
# extremely lightweight publish/subscribe messaging transport.
#
# https://www.home-assistant.io/integrations/mqtt
#
mqtt:
  binary_sensor: !include_dir_list ../entities/mqtt/binary_sensor
  sensor: !include_dir_list ../entities/mqtt/sensor
