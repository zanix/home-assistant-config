---
# This component can expose regular shell commands as services. Services can be
# called from a script or in automation. Shell commands aren’t allowed for a
# camel-case naming, please use lowercase naming only and separate the names
# with underscores.
#
# https://www.home-assistant.io/integrations/shell_command/
#
shell_command: !include_dir_merge_named ../components/shell_commands
