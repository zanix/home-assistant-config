###########################################################
# Recorder
###########################################################

purge_interval: 1
purge_keep_days: 7
db_url: !secret recorder_db_url
exclude:
  domains:
    - group
    - zwave
  entities:
    # System
    - sensor.date
    - sensor.ha_uptime

    # - sensor.since_last_boot
    - sensor.since_last_boot_templated
    - sensor.time
