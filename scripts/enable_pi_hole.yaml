---
# Runs a shell command to enable pihole.
#
# https://www.home-assistant.io/components/shell_command/
#
enable_pi_hole:
  alias: Enable Pi-Hole
  sequence:
    service: shell_command.pi_hole_enable
