---
# High level health status of UniFi Security Gateway devices via UniFi
# Controller.
#
# https://github.com/custom-components/sensor.unifigateway
#
platform: unifigateway
host: !secret unifigateway_host
username: !secret unifigateway_username
password: !secret unifigateway_password
