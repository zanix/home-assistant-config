---
# The template platform supports sensors which get their values from other
# entities.
#
# https://www.home-assistant.io/integrations/ring/
# https://www.home-assistant.io/integrations/template/
#
platform: template
sensors:
  ring_last_ding:
    friendly_name: Last Ring Time
    availability_template: "{{ true if not is_state('sensor.front_door_last_ding', 'unavailable') else false }}"
    value_template: "{{ as_timestamp(state_attr('sensor.front_door_last_ding', 'created_at')) | timestamp_custom('%m/%d/%Y %I:%M %p', True) }}"
    icon_template: mdi:bell-ring
