---
# The template platform supports sensors which get their values from other
# entities.
#
# https://www.home-assistant.io/integrations/ring/
# https://www.home-assistant.io/integrations/template/
#
platform: template
sensors:
  ring_last_motion:
    friendly_name: Last Motion Time
    availability_template: "{{ true if not is_state('sensor.front_door_last_motion', 'unavailable') else false }}"
    value_template: "{{ as_timestamp(state_attr('sensor.front_door_last_motion', 'created_at')) | timestamp_custom('%m/%d/%Y %I:%M %p', True) }}"
    icon_template: mdi:run-fast
