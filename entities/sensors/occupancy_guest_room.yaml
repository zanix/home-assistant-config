---
# The template platform supports sensors which get their values from other
# entities.
#
# https://www.home-assistant.io/integrations/ecobee/
# https://www.home-assistant.io/integrations/climate/
# https://www.home-assistant.io/integrations/template/
#
platform: template
sensors:
  occupancy_guest_room:
    friendly_name: Guest Bedroom Occupancy
    entity_id: binary_sensor.guest_room_occupancy
    availability_template: "{{ true if not is_state('binary_sensor.guest_room_occupancy', 'unavailable') else false }}"
    value_template: >-
      {%- if is_state("binary_sensor.guest_room_occupancy", "on") -%}
        Occupied
      {%- else -%}
        Clear
      {%- endif %}
    icon_template: >-
      {%- if is_state("binary_sensor.guest_room_occupancy", "on") -%}
        mdi:account
      {%- else -%}
        mdi:account-off
      {%- endif %}
