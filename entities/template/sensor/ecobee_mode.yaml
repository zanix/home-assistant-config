---
# The template platform supports sensors which get their values from other
# entities.
#
# https://www.home-assistant.io/integrations/ecobee/
# https://www.home-assistant.io/integrations/climate/
# https://www.home-assistant.io/integrations/template/
#
sensor:
  - name: Ecobee Mode
    availability: '{{ has_value("climate.home") or has_value("climate.ecobee") }}'
    state: |
      {% if has_value('climate.ecobee') and is_state_attr('climate.ecobee', 'hvac_action', 'fan') %}
        Fan
      {% else %}
        {{ state_attr('climate.home', 'hvac_action') | capitalize }}
      {% endif %}
