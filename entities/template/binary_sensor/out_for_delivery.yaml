---
# The template integration allows creating entities which derive their values
# from other data. This is done by specifying templates for properties of an
# entity, like the name or the state.
#
# https://www.home-assistant.io/integrations/template/
#
binary_sensor:
  - name: Out for Delivery
    availability: "{{ not is_state('sensor.mail_packages_in_transit', 'unavailable') or not is_state('sensor.mail_usps_mail', 'unavailable') }}"
    state: "{{ (states('sensor.mail_packages_in_transit')|int(default=0)) > 0 or (states('sensor.mail_usps_mail')|int(default=0)) > 0 }}"
    icon: mdi:package-variant-plus
