###########################################################
# Script: MQTT
###########################################################

notify_mqtt:
  sequence:
    - service: mqtt.publish
      data_template:
        payload: "{{ message }}"
        topic: "{{ topic }}"
        retain: true
