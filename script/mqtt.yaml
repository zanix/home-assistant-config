###########################################################
# Script: MQTT
#   topic: "topic/path/here"
#   message - "title|body|image"
###########################################################

notify_mqtt:
  sequence:
    - service: mqtt.publish
      data_template:
        payload: "{{ message }}"
        topic: "{{ topic }}"
        retain: true
