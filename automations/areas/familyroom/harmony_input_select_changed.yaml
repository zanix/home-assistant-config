---
# Changes the Harmony remote activity when the input select is changed.
#
alias: "[Harmony] Family Room Input Select Changed"
hide_entity: true
trigger:
  platform: state
  entity_id: input_select.remote_family_room
  from: Select Activity
action:
  service: script.turn_on
  entity_id: script.remote_family_room
