---
# Set the default theme on start up.
#
# This only affects browsers that do not have a theme set.
#
alias: "[System] Set Theme at Startup"
trigger:
  platform: homeassistant
  event: start
action:
  service: frontend.set_theme
  data:
    name: "midnight-remake"
